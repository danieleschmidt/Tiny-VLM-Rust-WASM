# Tiny-VLM Environment Configuration

# Model Configuration
# ===================
# Path to model weights directory
MODEL_PATH=./models
# Default model variant to load (tiny-vlm-25m, tiny-vlm-50m, tiny-vlm-100m)
DEFAULT_MODEL=tiny-vlm-25m
# Enable model quantization (int8, int4, none)
QUANTIZATION=int8

# Performance Settings
# ===================
# Maximum memory usage in MB
MAX_MEMORY_MB=512
# Number of CPU threads for inference
NUM_THREADS=4
# Enable SIMD optimizations (true/false)
ENABLE_SIMD=true
# Cache size for model weights in MB
MODEL_CACHE_SIZE_MB=100

# WASM Configuration
# ==================
# Enable WebAssembly SIMD (true/false)
WASM_SIMD=true
# WASM memory pages (each page is 64KB)
WASM_MEMORY_PAGES=1024
# Enable WASM threading
WASM_THREADS=false

# Development Settings
# ===================
# Log level (error, warn, info, debug, trace)
LOG_LEVEL=info
# Enable debug assertions in release builds
DEBUG_ASSERTIONS=false
# Enable performance profiling
ENABLE_PROFILING=false
# Directory for temporary files
TEMP_DIR=/tmp/tiny-vlm

# Benchmarking
# ============
# Number of warmup iterations for benchmarks
BENCHMARK_WARMUP=10
# Number of measurement iterations
BENCHMARK_ITERATIONS=100
# Enable accuracy benchmarking against reference implementation
BENCHMARK_ACCURACY=true
# Path to benchmark datasets
BENCHMARK_DATA_PATH=./data/benchmarks

# Cross-platform Testing
# =====================
# Test on WASM target (true/false)
TEST_WASM=true
# Test with different optimization levels
TEST_OPT_LEVELS=true
# Enable memory leak detection
TEST_MEMORY_LEAKS=false

# CI/CD Configuration
# ==================
# GitHub token for releases (CI only)
# GITHUB_TOKEN=your_token_here
# Enable automatic model downloads in CI
CI_DOWNLOAD_MODELS=false
# Slack webhook for notifications (optional)
# SLACK_WEBHOOK=https://hooks.slack.com/services/...

# Security Settings
# ================
# Maximum input image size in bytes (10MB default)
MAX_IMAGE_SIZE=10485760
# Maximum text prompt length in characters
MAX_PROMPT_LENGTH=2048
# Enable input sanitization
SANITIZE_INPUTS=true
# Rate limiting for API endpoints (requests per minute)
RATE_LIMIT=60

# Examples and Demos
# ==================
# Port for development server
DEV_SERVER_PORT=8000
# Port for example applications
EXAMPLE_APP_PORT=3000
# Enable hot reloading in development
HOT_RELOAD=true
# Path to example images
EXAMPLE_IMAGES_PATH=./data/examples

# Model Training (if enabled)
# ===========================
# Dataset path for training
# DATASET_PATH=./data/training
# Learning rate
# LEARNING_RATE=1e-4
# Batch size
# BATCH_SIZE=32
# Number of training epochs
# EPOCHS=10

# Monitoring and Telemetry
# ========================
# Enable telemetry collection (true/false)
ENABLE_TELEMETRY=false
# Telemetry endpoint URL
# TELEMETRY_ENDPOINT=https://telemetry.example.com
# Application name for telemetry
APP_NAME=tiny-vlm
# Environment name (dev, staging, prod)
ENVIRONMENT=dev